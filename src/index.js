import React from 'react';
import ReactDOM from 'react-dom';
import './index.scss';
import StudentRecordPage from './components/SRPage/StudentRecordPage';

ReactDOM.render(
  <React.StrictMode>
    <StudentRecordPage />
  </React.StrictMode>,
  document.getElementById('root')
);
